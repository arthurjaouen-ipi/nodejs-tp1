// Athlete routes
module.exports = function(app){
    // Create athlete
    app.post('/api/athletes', (req,res) => {
        
    })

    // Delete athlete
    app.delete('/api/athletes/:athleteId', (req,res) => {
        
    })

    // Update athlete
    app.put('/api/athletes/:athleteId', (req,res) => {
        
    })

    // All athletes
    app.get('/api/athletes', (req,res) => {
        
    })

    // Specific athlete
    app.get('/api/athletes/:athleteId', (req,res) => {
        
    })

    // All sports of specific athlete
    app.get('/api/athletes/:athleteId/sports', (req,res) => {
        
    })

    // All athletes of specific country
    app.get('/api/countries/:countryId/athletes', (req,res) => {
       
    })
}